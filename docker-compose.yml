version: "3.8"

services:
  flask-app:
    build: ./flask-app
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=development

  react-app:
    build: ./react-app
    ports:
      - "3005:80"
    depends_on:
      - flask-app
